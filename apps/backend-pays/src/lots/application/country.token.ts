// Token d'injection du code pays de l'instance (env COUNTRY_CODE). L'application
// reçoit une valeur (`CountryCode`) plutôt que ConfigService : elle reste pure.
export const COUNTRY_CODE = Symbol('COUNTRY_CODE');
