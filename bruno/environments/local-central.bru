vars {
  baseUrl: http://localhost:3000
  userEmail: admin@futurekawa.local
  userPassword: changeme
}

// refresh token = cookie httpOnly fk_refresh (cookie jar Bruno), pas une var.
vars:secret [
  accessToken
]
